import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parthkaushal.synapse',
  appName: 'Synapse AI News',
  webDir: 'build',
};

export default config;
